// Declare three variables using const and let: your name (string), your age (number), and a boolean called isStudent. Log all three to the console on separate lines.
const fullName = "Dimma Joel";
let age = 24;
let isStudent = false;
console.log(fullName);
console.log(age);
console.log(isStudent);
